import { Button } from "./components/ui/button.tsx";

const App = () => {
  return (
    <div className="uppercase underline mt-20">
      <Button variant="destructive">App</Button>
    </div>
  );
};

export default App;
